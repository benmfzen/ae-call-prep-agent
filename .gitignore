# Secrets — never commit
.env
*.env
.config/

# Internal working notes — not part of the submission (reworked separately for the presentation)
analysis/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store

# Keep the data snapshot in the repo (synthetic data, demo safety net) — do NOT ignore data/
